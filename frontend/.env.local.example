# Public — safe to expose to the browser. Points at the SatyaSetu backend API.
# Never put SARVAM_API_KEY or any other secret in a NEXT_PUBLIC_* variable.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8001
